#ifndef _gscollision_h_
#define _gscollision_h_

#include <stdio.h>

#include <gsgeom/gsmatrix.h>
#include <gsgeom/gsmesh.h>
#include <gsgeom/gsline.h>
#include <gsgeom/gsray.h>
#include <gsgeom/gsspline.h>
#include <gsgeom/gsgeometrychunk.h>
#include <gsgeom/gsvector.h>

namespace gscol
{
	/*
	class GS gscollision
	{
	public:
		static bool isRayIntersect (const gsgeom::gsray &r, const gsgeom::gsgeometrychunk *s);
		static gsgeom::gsgeometrybuffer *RayIntersectScene (const gsgeom::gsray &r, const gsgeom::gsgeometrychunk *s);
		static gsgeom::gsvec3f RayIntersectPoint (const gsgeom::gsray &r, const gsgeom::gsgeometrychunk *s);
		static gsgeom::gsvec3i RayIntersectIndex (const gsgeom::gsray &r, const gsgeom::gsgeometrychunk *s);
	};
	*/
};

#endif

